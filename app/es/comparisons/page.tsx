import Link from "next/link";
import { ArrowRight, BadgeCheck, BookOpenText, Clock3, FileText, Scale, Sparkles } from "lucide-react";

import { Container } from "@/components/ui/container";
import { Section } from "@/components/ui/section";
import { formatComparisonDate, getAllComparisons } from "@/lib/comparisons";
import { createSpanishPageMetadata } from "@/lib/i18n/metadata";
import { publicImageSrc } from "@/lib/public-image-src";

export const dynamic = "force-dynamic";

export const metadata = createSpanishPageMetadata({
  title: "Comparaciones y alternativas de apps Shopify",
  description:
    "Compara Hyper Apps de NiagaraT con alternativas Shopify usando criterios factuales y consideraciones practicas de implementacion.",
  path: "/es/comparisons",
});

const principles = [
  ["Primero el caso de uso", "Empieza por el problema de tienda, recorrido del cliente y necesidad operativa que quieres resolver."],
  ["Trade-offs claros", "Evalua diferencias importantes sin reducir todo a una lista de funciones."],
  ["Siguientes pasos practicos", "Sal con una guia para saber que verificar, evaluar e implementar despues."],
] as const;

const relatedLinks = [["/es/blog", "Leer el blog", "Perspectivas practicas sobre descubrimiento, conversion y comercio IA.", BookOpenText], ["/es/resources", "Usar recursos", "Convierte una decision en accion con guias y playbooks.", FileText]] as const;

export default async function SpanishComparisonsPage() {
  const comparisons = await getAllComparisons();
  return (
    <>
      <Section spacing="none" className="pb-6 pt-10 sm:pb-8 sm:pt-14 lg:pt-16"><Container className="max-w-6xl"><div className="relative overflow-hidden rounded-3xl border border-border bg-surface px-6 py-7 shadow-[0_28px_70px_-46px_hsl(var(--shadow)/0.72)] sm:px-10 sm:py-9"><div className="relative grid gap-6 lg:grid-cols-[minmax(0,1fr)_16rem] lg:items-end"><div><div className="inline-flex items-center gap-2 rounded-full border border-border bg-background/80 px-3 py-1.5 text-xs font-semibold uppercase tracking-[0.16em] text-muted-foreground"><Sparkles aria-hidden="true" className="size-3.5 text-primary" />Guias de decision</div><p className="mt-4 text-sm font-semibold uppercase tracking-[0.22em] text-primary">Comparaciones Shopify</p><h1 className="mt-3 max-w-4xl type-display">Compara apps Shopify con contexto de producto mas claro.</h1><p className="mt-4 max-w-3xl text-base leading-7 text-muted-foreground sm:text-lg sm:leading-8">Explora guias de alternativas y versus para comerciantes Shopify que comparan Hyper Apps con otras herramientas. Cada comparacion aclara categoria, audiencia, casos de uso y lo que el producto Hyper esta construido para resolver.</p></div><aside className="rounded-2xl border border-border bg-background/75 p-5 backdrop-blur sm:p-6"><Scale aria-hidden="true" className="size-5 text-primary" /><p className="mt-4 text-4xl font-semibold tracking-tight">{comparisons.length}</p><p className="mt-1 text-sm font-semibold text-foreground">{comparisons.length === 1 ? "guia" : "guias"} de comparacion</p><p className="mt-2 text-sm leading-6 text-muted-foreground">Para hacer la evaluacion mas clara, sin reemplazar la debida diligencia de tu equipo.</p></aside></div></div></Container></Section>
      <Section spacing="none" className="pb-6 sm:pb-8"><Container className="max-w-6xl"><div className="grid gap-4 md:grid-cols-3">{principles.map(([title, description], index) => (<article key={title} className="rounded-2xl border border-border bg-surface p-5 shadow-sm"><span className="inline-flex size-9 items-center justify-center rounded-xl border border-border bg-background text-sm font-semibold text-primary">0{index + 1}</span><h2 className="mt-4 text-lg font-semibold tracking-tight">{title}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{description}</p></article>))}</div></Container></Section>
      <Section spacing="none" className="pb-8 sm:pb-10"><Container className="max-w-6xl"><div className="flex flex-col gap-3 border-b border-border pb-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold uppercase tracking-[0.2em] text-primary">Biblioteca de comparaciones</p><h2 className="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">Encuentra la comparacion Shopify que necesitas.</h2></div><p className="max-w-xl text-sm leading-6 text-muted-foreground sm:text-right">Explora comparaciones practicas para evaluar soluciones Shopify con criterios claros.</p></div>{comparisons.length ? <div className="mt-6 grid gap-5 md:grid-cols-2">{comparisons.map((comparison) => { const displayDate = comparison.updatedAt ?? comparison.publishedAt; return <Link key={comparison.slug} href={`/comparisons/${comparison.slug}`} className="group flex h-full flex-col rounded-2xl border border-border bg-surface p-6 shadow-sm transition-all hover:-translate-y-1 hover:border-primary/45 sm:p-7">{comparison.coverImage ? <div className="-mx-6 -mt-6 mb-6 overflow-hidden border-b border-border bg-muted sm:-mx-7 sm:-mt-7"><img src={publicImageSrc(comparison.coverImage)} alt={comparison.title} className="aspect-[16/9] w-full object-cover transition duration-300 group-hover:scale-[1.03]" loading="lazy" /></div> : null}<div className="flex items-start justify-between gap-4"><span className="inline-flex size-11 items-center justify-center rounded-xl border border-border bg-muted/60 text-primary"><Scale aria-hidden="true" className="size-5" /></span><time dateTime={displayDate} className="rounded-full border border-border bg-background px-3 py-1.5 text-xs font-medium text-muted-foreground">{formatComparisonDate(displayDate)}</time></div><p className="mt-6 text-xs font-semibold uppercase tracking-[0.16em] text-primary">{comparison.category}</p><h3 className="mt-3 text-2xl font-semibold tracking-tight transition-colors group-hover:text-primary">{comparison.title}</h3><p className="mt-3 text-sm leading-7 text-muted-foreground">{comparison.decisionSummary || comparison.excerpt}</p><div className="mt-auto flex flex-col gap-3 border-t border-border pt-4 sm:flex-row sm:items-center sm:justify-between"><span className="inline-flex items-center gap-2 text-sm text-muted-foreground"><Clock3 aria-hidden="true" className="size-4" />{comparison.readingTime} min de lectura</span><span className="inline-flex items-center gap-2 text-sm font-semibold text-foreground transition-colors group-hover:text-primary">Leer comparacion<ArrowRight aria-hidden="true" className="size-4" /></span></div></Link>; })}</div> : <div className="mt-6 grid place-items-center rounded-2xl border border-dashed border-border bg-surface px-6 py-12 text-center"><Scale aria-hidden="true" className="size-8 text-primary" /><h3 className="mt-4 text-2xl font-semibold tracking-tight">Las comparaciones se estan preparando.</h3><p className="mt-2 max-w-xl text-sm leading-6 text-muted-foreground">Guias practicas para decisiones Shopify apareceran pronto.</p></div>}</Container></Section>
      <Section spacing="none" className="pb-12 sm:pb-16"><Container className="max-w-6xl"><div className="rounded-3xl border border-border bg-surface p-5 shadow-sm sm:p-6"><div className="flex flex-col gap-3 border-b border-border pb-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold uppercase tracking-[0.2em] text-primary">Planear despues de comparar</p><h2 className="mt-2 text-2xl font-semibold tracking-tight sm:text-3xl">Reune contexto y luego actua.</h2></div><BadgeCheck aria-hidden="true" className="size-6 text-primary" /></div><div className="mt-5 grid gap-4 md:grid-cols-2">{relatedLinks.map(([href, label, description, Icon]) => (<Link key={href} href={href} className="group rounded-2xl border border-border bg-background p-5 transition-all hover:-translate-y-0.5 hover:border-primary/40"><span className="inline-flex size-10 items-center justify-center rounded-xl border border-border bg-surface text-primary"><Icon aria-hidden="true" className="size-5" /></span><h3 className="mt-4 text-lg font-semibold tracking-tight transition-colors group-hover:text-primary">{label}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{description}</p></Link>))}</div></div></Container></Section>
    </>
  );
}
